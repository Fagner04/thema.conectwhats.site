{%- comment -%}
  TESTE DE NOTIFICAÇÃO DE ESTOQUE
  Este snippet mostra informações de debug
{%- endcomment -%}

<div style="background: #fff3cd; border: 2px solid #ffc107; padding: 15px; margin: 20px 0; border-radius: 8px;">
  <h4 style="margin: 0 0 10px 0; color: #856404;">🔍 Debug - Notificação de Estoque</h4>
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Configuração ativada:</strong> 
    {% if section.settings.enable_back_in_stock_notification %}
      ✅ SIM
    {% else %}
      ❌ NÃO (Ative nas configurações da seção)
    {% endif %}
  </p>
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Produto ID:</strong> {{ product.id }}
  </p>
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Variante selecionada:</strong> {{ selected_variant.title }}
  </p>
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Disponível:</strong> 
    {% if selected_variant.available %}
      ✅ SIM (botão não deve aparecer)
    {% else %}
      ❌ NÃO (botão DEVE aparecer)
    {% endif %}
  </p>
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Estoque:</strong> {{ selected_variant.inventory_quantity }}
  </p>
  
  <hr style="margin: 10px 0; border: none; border-top: 1px solid #ffc107;">
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Botão ID esperado:</strong> notify-trigger-{{ product.id }}
  </p>
  
  <p style="margin: 5px 0; font-size: 13px;">
    <strong>Modal ID esperado:</strong> back-in-stock-modal-{{ product.id }}
  </p>
  
  <button 
    onclick="
      const btn = document.getElementById('notify-trigger-{{ product.id }}');
      const modal = document.getElementById('back-in-stock-modal-{{ product.id }}');
      alert('Botão encontrado: ' + (btn ? 'SIM' : 'NÃO') + '\\nModal encontrado: ' + (modal ? 'SIM' : 'NÃO'));
    "
    style="background: #007bff; color: white; border: none; padding: 8px 15px; border-radius: 4px; cursor: pointer; font-size: 12px; margin-top: 10px;"
  >
    Testar se elementos existem
  </button>
  
  <button 
    onclick="openBackInStockModal({{ product.id }})"
    style="background: #28a745; color: white; border: none; padding: 8px 15px; border-radius: 4px; cursor: pointer; font-size: 12px; margin-top: 10px; margin-left: 5px;"
  >
    Forçar abrir modal
  </button>
</div>
